rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lokal im Emulator: alles erlaubt
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
